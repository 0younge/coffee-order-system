spring:
  application:
    name: coffee-order-system
  config:
    import: "optional:file:.env[.properties]"
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:${DB_PORT:3307}/${DB_NAME:coffee_order_system}}
    username: ${DB_USERNAME:coffee}
    password: ${DB_PASSWORD}
